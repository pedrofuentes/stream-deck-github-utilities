# Agent Instructions — Stream Deck GitHub Utilities

This file provides instructions for AI coding agents (Copilot, Cursor, Cline, etc.) contributing to this project.

## Project Overview

This is a **Stream Deck plugin** built with TypeScript using the official **Elgato Stream Deck SDK v2** and **Stream Deck CLI**. It displays GitHub-related information on Stream Deck devices.

- **Plugin UUID:** `com.pedrofuentes.github-utilities`
- **Repository:** https://github.com/pedrofuentes/stream-deck-github-utilities
- **SDK Docs:** https://docs.elgato.com/streamdeck/sdk/introduction/getting-started/
- **CLI Docs:** https://docs.elgato.com/streamdeck/cli/intro

## Architecture

```
src/
├── plugin.ts          # Entry point — registers actions and connects to Stream Deck
├── actions/           # One file per Stream Deck action (class-based)
│   ├── repo-stats.ts      # Repository statistics display
│   └── workflow-status.ts # Workflow run + deployment status display
└── utils/             # Shared utilities (GitHub API, formatting, validation)

com.pedrofuentes.github-utilities.sdPlugin/
├── manifest.json      # Plugin metadata, action definitions, platform requirements
├── bin/               # Compiled output (DO NOT EDIT — generated by Rollup)
├── imgs/              # Icons/images referenced by manifest
├── ui/                # Property inspector HTML files
└── logs/              # Runtime logs (gitignored)

tests/                 # Test files mirroring src/ structure (262 tests)
```

## Critical Rules

### 1. Tests Are Mandatory
- **Every code change MUST include corresponding tests.**
- Test happy paths, edge cases (null, undefined, empty strings, boundary values), and error scenarios.
- Tests use **Vitest** — config is in `vitest.config.ts`.
- Coverage thresholds: 80% branches, functions, lines, statements.
- Place test files in `tests/` mirroring the `src/` directory structure.
- Test files must use `.test.ts` or `.spec.ts` extension.

### 2. All Tests Must Pass Before Packaging
- The `npm run pack` command runs tests before creating the release package.
- **Never skip or disable tests to make a build pass.**
- If a test fails, fix the code or the test — do not delete the test.

### 3. Always Update Documentation
- Update `README.md` when adding features, actions, or changing the public API.
- Update `CONTRIBUTING.md` when changing development workflows or requirements.
- Use JSDoc comments on all exported functions and classes.

## How to Add a New Action

1. **Create the action class** in `src/actions/<action-name>.ts`:
   - Extend `SingletonAction<YourSettings>` from `@elgato/streamdeck` for single-instance actions.
   - Settings interfaces **must** include `[key: string]: JsonValue` index signature (import from `@elgato/utils`).
   - Use the `@action` decorator with UUID pattern: `com.pedrofuentes.github-utilities.<action-name>`
   - Implement `onWillAppear`, `onWillDisappear`, `onKeyDown`, `onDidReceiveSettings` as needed.
   - For polling actions, use `setInterval` in `onWillAppear` and `clearInterval` in `onWillDisappear`, keyed by `ev.action.id`.

2. **Register the action** in `src/plugin.ts`:
   ```typescript
   import { MyAction } from "./actions/my-action";
   streamDeck.actions.registerAction(new MyAction());
   ```

3. **Add to the manifest** in `com.pedrofuentes.github-utilities.sdPlugin/manifest.json`:
   ```json
   {
     "Actions": [
       {
         "Icon": "imgs/actions/my-action/icon",
         "Name": "My Action",
         "States": [{ "Image": "imgs/actions/my-action/key" }],
         "UUID": "com.pedrofuentes.github-utilities.my-action",
         "PropertyInspectorPath": "ui/my-action.html"
       }
     ]
   }
   ```

4. **Create property inspector** in `com.pedrofuentes.github-utilities.sdPlugin/ui/<action-name>.html`:
   - Use `sdpi-components.js` for UI components (already in `ui/` folder).
   - Use `setting="propertyName"` for per-action settings.
   - Use `global` attribute for plugin-wide settings (e.g., GitHub token).

5. **Create icons** in `com.pedrofuentes.github-utilities.sdPlugin/imgs/actions/<action-name>/`:
   - `icon.svg` — 20x20 for the action list in Stream Deck
   - `key.svg` — 144x144 default button appearance

6. **Write tests** in `tests/actions/<action-name>.test.ts`:
   - Mock `@elgato/streamdeck` with `vi.hoisted()` + `vi.mock()` (see `repo-stats.test.ts` for pattern).
   - Test all event handlers, error states, and edge cases.

7. **Update README.md** with the new feature in the Features section.

## Existing Patterns to Follow

### Button Rendering
- Use `src/utils/button-renderer.ts` for SVG button generation.
- All buttons use 144×144 SVG with GitHub dark theme.
- **SVG encoding**: `ev.action.setImage("data:image/svg+xml," + encodeURIComponent(svg))` — do NOT use `charset=utf8` or base64; only `encodeURIComponent` renders correctly on Stream Deck hardware.
- Clear title with `ev.action.setTitle("")` when using full SVG buttons.
- Manifest must set `"ShowTitle": false` in each action's States to prevent default title overlay.
- **Text buttons**: `renderKeyImage()` for generic layout, `renderStatImage()` for repo stats, `renderWorkflowImage()` for workflow status, `renderDeployingImage()` for active deployments.
- **Icon buttons**: `renderIconKeyImage()` for buttons with centered SVG icons (used by workflow status and deploying views).
- **Status icons**: `STATUS_ICONS` record contains 14 SVG path definitions (36×36 viewBox) for workflow statuses: `success`, `failure`, `in_progress`, `cancelled`, `queued`, `pending`, `waiting`, `skipped`, `timed_out`, `action_required`, `neutral`, `stale`, `requested`, `deploying`.
- **Device compatibility**: Do NOT use nested `<svg>` elements — Stream Deck's renderer does not support them. Use `<g transform="translate(x,y) scale(s)">` to position and scale icon paths instead.
- Render states: stat display, workflow status (with icon), deploying (with icon), loading, error, unconfigured.

### GitHub API
- Use `src/utils/github-api.ts` for all GitHub REST API calls.
- Global token from `streamDeck.settings.getGlobalSettings<GlobalSettings>()`.
- Handle all error codes: 401, 403 (rate limit vs. access denied), 404.
- Use `src/utils/github.ts` for token validation, repo parsing, count formatting.
- Use `handleApiError()` for centralized error handling in new API functions.

### Workflow Status Patterns
- `fetchWorkflowInfo()` fetches both workflow runs and deployment status in parallel.
- Workflow run errors propagate (for proper error display) while deployment errors are caught gracefully.
- Active deploy states (`in_progress`, `queued`, `pending`) trigger the deploying button view.
- Status display priority: deploying > latest workflow run > "No Runs".
- Status-to-label mapping via `getWorkflowStatusLabel()`, status-to-color via `getWorkflowStatusColor()`.
- `renderWorkflowButton()` for run status view, `renderDeployingButton()` for active deployment view.
- Default refresh interval is 60s (vs 300s for repo-stats) since workflows change faster.
- Minimum refresh interval is 15s.

### Settings Types
- Define in `src/types.ts`.
- Must extend `JsonObject` via index signature: `[key: string]: JsonValue`.
- Import `JsonValue` from `@elgato/utils`.

## How to Add Utility Functions

1. Add to the appropriate file in `src/utils/` or create a new file.
2. Export from `src/utils/index.ts`.
3. Add JSDoc documentation.
4. Write comprehensive tests in `tests/utils/`.

## Coding Standards

- **Language:** TypeScript only — no plain JS in `src/`.
- **Module System:** ES modules (`import`/`export`).
- **Indentation:** Tabs.
- **String quotes:** Double quotes.
- **Strict mode:** Enforced by tsconfig (`strict: true`).
- **No unused variables or parameters** — enforced by compiler.
- **Explicit return types** on all exported functions.

## Key Dependencies

| Package | Purpose |
|---|---|
| `@elgato/streamdeck` | Stream Deck SDK — core plugin API |
| `rollup` | JavaScript bundler |
| `@rollup/plugin-typescript` | TypeScript compilation in Rollup |
| `@rollup/plugin-node-resolve` | Node module resolution for Rollup |
| `vitest` | Test runner |
| `@vitest/coverage-v8` | Code coverage |

## npm Scripts Reference

| Script | What It Does |
|---|---|
| `npm run build` | Compile and bundle via Rollup |
| `npm run watch` | Build + restart plugin on changes |
| `npm test` | Run full test suite (must pass before release) |
| `npm run test:watch` | Tests in watch mode |
| `npm run test:coverage` | Tests with coverage report |
| `npm run lint` | ESLint check |
| `npm run validate` | Stream Deck CLI validation |
| `npm run pack` | Build + test + validate + package `.streamDeckPlugin` |

## Release Process

1. All tests pass: `npm test`
2. Build succeeds: `npm run build`
3. Plugin validates: `npm run validate`
4. Package: `streamdeck pack com.pedrofuentes.github-utilities.sdPlugin --output dist/`

The resulting `.streamDeckPlugin` file in `dist/` is the distributable installer.

## Common Pitfalls

- **Do not edit files in `com.pedrofuentes.github-utilities.sdPlugin/bin/`** — they are generated.
- **Image paths in manifest omit extensions** — use `"imgs/plugin-icon"` not `"imgs/plugin-icon.svg"`.
- **Plugin UUID format is strict** — lowercase alphanumeric, hyphens, periods only.
- **The `manifest.json` must pass `streamdeck validate`** before packaging.
- **GitHub tokens should never be logged in plaintext** — use `maskToken()` from utils.

---

## UI & Design Guidelines

These guidelines cover both **Stream Deck button design** (SVG on OLED hardware) and **Property Inspector / Settings** design (HTML panels in the Stream Deck desktop app). They draw from the Claude Code frontend design skill, OLED display best practices, and Elgato SDK conventions.

### Design Thinking Process

Before designing any UI element, follow this process:

1. **Understand the purpose** — What information must the user get at a glance? For buttons: status, number, label. For settings: configuration + feedback.
2. **Pick a bold aesthetic direction** — Don't default to generic. GitHub's dark theme is our foundation — lean into it with confidence.
3. **Consider constraints** — Stream Deck keys are physically 72×72 pixels (rendered at 144×144). OLED displays are _tiny_. Every pixel counts.
4. **Find differentiation** — What makes this recognizable vs. other plugins? Strong use of accent colors, clean iconography, and information density.

### Stream Deck Button Design (SVG, 144×144 → 72×72 OLED)

#### Key Constraints
- **Canvas size**: 144×144 SVG viewport, rendered on 72×72 physical OLED pixels
- **Viewing distance**: ~60cm (arm's length) — must be readable at a glance
- **OLED characteristics**: True black (`#000000`) is off/unlit; high contrast is free; glow/bloom effects look stunning
- **Animation**: SVG `<animate>` and `<animateTransform>` are supported for loading/progress indicators

#### Visual Hierarchy (Order of Importance)
1. **Primary data** (count number, status) — largest, boldest, center of canvas
2. **Status color** — immediate visual cue (green = good, red = bad, yellow = in-progress)
3. **Icon** — reinforces type/status, supports the primary data
4. **Label** (stat type, workflow name) — secondary, smaller
5. **Metadata** (repo name, environment) — smallest, muted, bottom of canvas

#### Typography Rules
- **Primary numbers**: 36–40px, bold, white (`#e6edf3`)
- **Status labels**: 14–16px, bold, accent color
- **Secondary labels**: 10–11px, accent color or white
- **Metadata text**: 9–10px, muted (`#8b949e`)
- **Font**: Use `"SF Pro", "Segoe UI", Arial, Helvetica, sans-serif` — system fonts that render cleanly at small sizes
- **Never use thin/light weights** — they disappear on small displays
- **Truncation**: Always truncate long text with `..` to prevent overflow; repo names max 16 chars, workflow names max 18 chars

#### Color Palette (GitHub Dark Theme)
```
Background:     #0d1117 (dark base)
Surface:        #161b22 (elevated surfaces)
Text Primary:   #e6edf3 (white-ish)
Text Muted:     #8b949e (gray)
Border:         #30363d

Accent – Stars:     #e3b341 (gold)
Accent – Issues:    #3fb950 (green)
Accent – Forks:     #58a6ff (blue)
Accent – Watchers:  #d2a8ff (purple)

Status – Success:      #3fb950
Status – Failure:      #f85149
Status – In Progress:  #d29922
Status – Queued:       #58a6ff
Status – Cancelled:    #8b949e
Status – Deploying:    #a371f7
```

#### OLED-Specific Best Practices
- **Use true black backgrounds** — saves power, looks premium, infinite contrast
- **Prefer filled shapes over outlined** — outlines with stroke-width <3 get lost at 72px
- **Use color-filled status indicators** — a solid green circle communicates faster than a green outline
- **Add subtle glow/radial effects** — OLED renders these beautifully; use radial gradients with low-opacity accent colors behind icons
- **Limit text to 2–3 lines** — anything more is illegible at this size
- **Avoid fine details** — thin lines, small dots, intricate patterns all degrade

#### Button Layout Pattern
```
┌──────────────────┐
│  [colored icon]   │  12–50px from top (icon area)
│                   │
│     42.5k         │  center (primary data, large bold)
│                   │
│     Stars         │  secondary label (accent colored)
│   owner/repo      │  metadata (muted, small)
└──────────────────┘
```

#### Animation Guidelines
- Use `<animate>` for loading states (opacity pulse on dots)
- Use `<animateTransform type="rotate">` for spinning progress indicators
- Keep animation duration 1.2–2s for comfortable pacing
- Stagger animation `begin` values for sequential dot effects

### Property Inspector & Settings Design

#### General Principles
- Follow the **sdpi-components** v4 visual language — dark background, light text
- Use `<sdpi-item>`, `<sdpi-select>` etc. for consistency with other Stream Deck plugins
- Custom styles should complement, not fight, the sdpi theme
- Keep sections clearly separated with headings and `<hr>` dividers
- Use status indicators (colored text) for feedback: green = success, red = error, yellow = loading, gray = idle

#### Setup Popup (Global Settings)
- Modal-like popup window opened via `window.open()`
- Communicates with parent PI via `window.opener._sdWebSocket`
- Uses system dark theme (`background: #2d2d2d`, `color: #e0e0e0`)
- Section headings: uppercase, small, letter-spaced, muted color
- Buttons: primary (blue `#0078ff`) for main actions, secondary (gray `#3a3a3a`) for others
- Token input: monospace font, password type with show/hide toggle
- Status area: colored text block with detailed validation feedback

#### Feedback & Interaction Patterns
- **Immediate validation feedback** — after token save, show success/error within 300ms
- **Loading states** — show "Validating…" or "Loading…" in yellow/amber
- **Error detail** — explain what went wrong and what action to take
- **Token status in PI** — always visible, updates on data changes (MutationObserver on dropdowns)
- **Dropdown refresh** — `show-refresh` attribute, `_refreshDropdowns()` callback from popup

### Anti-Patterns to Avoid
- ❌ Generic/cookie-cutter designs — no "AI aesthetic" (purple gradients on white)
- ❌ Thin, decorative fonts — illegible on small displays
- ❌ Low-contrast text — especially on OLED where muted text can vanish
- ❌ Too much information — force prioritization, not information overload
- ❌ Complex SVG paths at small scale — simplify icons for 72px
- ❌ Inline styles in PI HTML where CSS classes would suffice
- ❌ Unstyled native HTML elements — always match the dark theme

## Contributing Learnings Back to the Template

This plugin is part of a family of Stream Deck plugins that share a common template and knowledge base:

- **Template repo**: https://github.com/pedrofuentes/stream-deck-template
- **Local path**: `S:\Pedro\Projects\stream-deck-template`

As an agent working on this plugin, you should **proactively offer to contribute new learnings** back to the template after completing significant work. This helps all future plugins benefit from discoveries made here.

### How to contribute

1. Write new findings to `S:\Pedro\Projects\stream-deck-template\contributions\github-utilities.md` using the format documented in `S:\Pedro\Projects\stream-deck-template\COLLABORATION.md`.
2. Commit and push (or open a PR) to `https://github.com/pedrofuentes/stream-deck-template` with a conventional commit: `docs(github-utilities): add learnings about <topic>`.

### When to offer

- After solving a non-obvious bug or hardware quirk
- After implementing a reusable pattern (polling, caching, UI component)
- After discovering a manifest or SDK constraint
- After a release
- When the session is wrapping up

### Before starting major work

Read `S:\Pedro\Projects\stream-deck-template\LEARNINGS.md` for the latest consolidated knowledge from all plugins.
